---
// import EventCard from "../components/EventCard.astro";
import "../styles/global.css";
// import ruFlyer from "../../public/img/ru.jpg";
// import cd13M from "../../public/img/after-hours-001.jpg";
import BaseLayout from "../layouts/BaseLayout.astro";
import ButtonHome from "../components/ButtonHome.astro";
// import SMPTEBars from "../components/SMPTEBars.astro";

const events = [
  // {
    // date: "13 de marzo",
    // venue: "Le Musée",
    // city: "Valencia, VE",
    // price: "5€",
    // image: cd13M,
    // url: "https://www.instagram.com/p/DUUEHmuAcr6/",
  // }
];
---

<BaseLayout mainText="Calendario" subText="" showButtonHome={false} bgType="smpte">
  <!-- <div class="flex flex-col lg:flex-row gap-4 w-full md:w-8/12 lg:w-6/12 xl:w-4/12 mx-auto">
    {
      events.map((event) => (
        <EventCard
          date={event.date}
          venue={event.venue}
          city={event.city}
          price={event.price}
          image={event.image}
          url={event.url}
        />
      ))
    }
  </div> -->

  <div class="flex flex-col items-center justify-center flex-1 py-10 w-full min-h-[45vh]">
    <div class="flex flex-col items-center justify-center gap-4">
      <p class="font-mono text-2xl font-normal tracking-widest uppercase text-white bg-black/80 backdrop-blur-sm px-6 py-3 rounded-md shadow-2xl animate-pulse border border-white/10">
        VUELVE PRONTO 👽
      </p>
    </div>
  </div>

  <div class="w-full md:w-8/12 lg:w-6/12 xl:w-4/12 mx-auto mt-auto">
    <ButtonHome />
  </div>
</BaseLayout>
