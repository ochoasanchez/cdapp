---
import Head from '../components/Head.astro';
import GrainOverlay from '../components/GrainOverlay.astro';
import Header from '../components/Header.astro';
import ButtonStart from '../components/ButtonStart.astro';

interface Props {
  mainText?: string;
  subText?: string;
  showButtonStart?: boolean; // New optional boolean prop
}

const { mainText = "Club Doméstico", subText = "Electro y afecto en formato DIY 👽", showButtonStart = true } = Astro.props;
---
<html lang="en">
  <Head />
  <body
    class="flex flex-col items-center md:justify-center min-h-dvh w-full bg-red-800"
  >
    <div class="fixed inset-0 z-[-2] bg-red-800 "></div>
    <GrainOverlay />

    <main class="flex flex-col w-full gap-4 relative z-10 px-4 py-6">
      <Header mainText={mainText} subText={subText} />

      <slot />

      {showButtonStart && <ButtonStart />}
    </main>
  </body>
</html>
