---
// No props needed — this is just a loading placeholder
---
<div
  class="block bg-black rounded-lg text-white overflow-hidden border border-gray-800 w-full skeleton-card"
  aria-hidden="true"
>
  <!-- Image Skeleton -->
  <div class="w-full h-72 bg-gray-800 skeleton skeleton-img" />

  <!-- Content -->
  <div class="p-4 flex flex-col gap-4">
    <!-- Top Row: Date and Price -->
    <div class="flex justify-between items-center">
      <div class="h-6 w-40 rounded skeleton" />
      <div class="h-6 w-16 rounded skeleton" />
    </div>

    <!-- Venue and City -->
    <div class="h-4 w-32 rounded skeleton" />
    <div class="h-4 w-24 rounded skeleton" />
  </div>
</div>
<style>
  /* Skeleton shimmer animation */
.skeleton {
  background-color: #2a2a2a;
   background: linear-gradient(to right, transparent,#C0C0C0, gray);
  background-size: 200% 200%;
  background-repeat: no-repeat;
  background-position: left -100% top 0;
  animation: skeleton-shimmer 1.2s ease-in-out infinite;
}

@keyframes skeleton-shimmer {
  100% {
    background-position: right -100% top 0;
  }
}

/* Optional: control layout dimensions */
.skeleton-card {
  max-width: 100%;
}

.skeleton-img {
  position: relative;
}

</style>